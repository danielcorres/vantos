# Copia a .env.staging (ese archivo NO se sube a git) y rellena la contraseña de la BD de staging.
# Dashboard Supabase → Project Settings → Database → Database password
# Si la contraseña tiene # u otros caracteres especiales, usa comillas simples:
# SUPABASE_DB_PASSWORD='tu-password-aqui'
SUPABASE_DB_PASSWORD=
